discord:
  token: ""           # or set DISCORD_TOKEN env var
  guildId: ""
  channelId: ""
  clientId: ""

uptimeKuma:
  url: ""
  username: ""         # API token name, or username for basic auth
  apiKey: ""           # API token key, or password for basic auth (or set UPTIME_KUMA_API_KEY env var)

updateInterval: 60     # seconds
clearMessages: true    # clear bot's previous messages on startup

# Global status emojis (all optional, these are the defaults)
# emojis:
#   online: "🟢"
#   offline: "🔴"
#   warning: "🟡"
#   maintenance: "🔵"
#   unknown: "❓"

monitors:
  caseSensitive: true
  sections:
    - title: Gaming
      filters:
        - Lobby
        - Skyblock
        - Survival
    - title: Web
      filters:
        - regex: "^web\\d+$"    # regex filter
        - cdn                    # exact match filter
        - name: api             # exact match with custom emojis
          emojis:
            online: "✅"
            offline: "❌"
